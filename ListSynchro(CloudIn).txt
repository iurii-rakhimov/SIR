// ListSynchro[CloudIn].txt
MF = "MF_"
DataTxt = "_Data.txt"
YearMonth = SynchroTitle.text
strYearMonth = GetComponentString(YearMonth, " ", 1)
arrayDate = Components(strYearMonth, ".")
Year = arrayDate[0]
Month = arrayDate[1]
NameFile = MF + Year + Month + DataTxt
Path = GetPathDirectoryDocument()
PathAndNameFile = Path + NameFile
RecordsInDevice = FileRead(PathAndNameFile)
if RecordsInDevice == ""
{
	if RecordsInCloud == ""
	{
		Title.text = "Нет данных"
		return
	}
}
collectionBase = "MyFinance"
FirebaseAddRecord(collectionBase, strYearMonth, RecordsInDevice)
HideObject("CloudIn")
SynchroTitle.text = "Записали. Нажми иконку Обновить"
